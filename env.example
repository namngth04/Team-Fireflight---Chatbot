# Database
DATABASE_URL=postgresql://postgres:your_password@localhost:5433/chatbot_db

# JWT
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# LLM - Gemini (Primary Provider)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash  # Primary model (gemini-2.5-flash)
# Fallback models: Tự chọn trong code (default: gemini-2.0-flash-exp, gemini-1.5-pro)
# Có thể thay đổi trong app/services/rag_graph_service.py

# LLM - Ollama (Fallback Provider, Optional)
# Ollama là LLM miễn phí, chạy local, không cần API key
# Để sử dụng Ollama fallback:
# 1. Cài đặt Ollama: https://ollama.ai/
# 2. Pull model: ollama pull qwen2.5:7b (khuyến nghị cho tiếng Việt)
#    Hoặc model khác: ollama pull llama3, ollama pull mistral, ollama pull gemma:7b
# 3. Start Ollama server: ollama serve (chạy trên port 11434, thường tự động chạy sau khi cài)
# 4. Enable Ollama: OLLAMA_ENABLED=true (trong dự án thực tế, set true; example để false)
# 5. Cấu hình model: OLLAMA_MODEL=qwen2.5:7b (khuyến nghị cho tiếng Việt)
# 
# Model khuyến nghị cho tiếng Việt:
# - qwen2.5:7b (tốt nhất cho tiếng Việt, nhẹ, nhanh, khoảng 4.4GB)
# - llama3 (tốt nhưng hỗ trợ tiếng Việt kém hơn, khoảng 4.7GB)
# - mistral (tốt nhưng hỗ trợ tiếng Việt kém hơn, khoảng 4.1GB)
# - gemma:7b (tốt nhưng hỗ trợ tiếng Việt kém hơn, khoảng 5.0GB)
#
# Ollama không bắt buộc, có thể disable nếu không cần fallback
# Trong dự án thực tế: OLLAMA_ENABLED=true (enable để tránh rate limit)
# Trong example: OLLAMA_ENABLED=false (disable để không bắt buộc setup)
OLLAMA_ENABLED=false
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_API_KEY=ollama
OLLAMA_MODEL=qwen2.5:7b

# LLM - Retry Configuration
LLM_RETRY_ATTEMPTS=3
LLM_RETRY_BASE_DELAY=2.0
LLM_RETRY_MAX_DELAY=60.0

# File Storage
FILE_STORAGE_PATH=./storage
MAX_FILE_SIZE=52428800

# Application
DEBUG=true
SECRET_KEY=your_secret_key_here

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# MCP Server
MCP_SERVER_ENABLED=true
MCP_SERVER_PORT=8001

# ChromaDB
CHROMADB_PATH=./chroma_db
